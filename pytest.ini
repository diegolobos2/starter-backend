[pytest]
markers =
    race_demo: demostración didáctica de la condición de carrera (RET-001). Falla a propósito en Semana 1; se ejecuta por separado con: pytest -m race_demo
addopts = -m "not race_demo"
